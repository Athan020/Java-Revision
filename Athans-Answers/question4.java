/**
 * name
 */
public class question4{

    public static void main(String[] args) {
        String h1 = "Bob";
        String h2 = new String("Bob");
        /*B*/if (h1. equals(h2))

        
        /*
        The == operator compares refrences and not values so this would be the incorrect syntax for string values
        for strings we use the equals method to compare the values
        */
    }
}